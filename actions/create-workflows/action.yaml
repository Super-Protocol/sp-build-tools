name: Create workflow
description: Create workflow with spctl tool and return created order numbers

inputs:
  spctl_config_file:
    description: Path to spctl config file
    default: ./config.json
  tee_offers:
    description: tee offers separated by comma (eg. 1,3)
    required: true
  base_image:
    description: Base image offer number
  solution:
    description: Solution offer number or path to file with solution resource
    required: true
  solution_configuration:
    description: Path to solution configuration file or multiple files separated by comma
  data:
    description: Data offer number (with or without slot) or path to data resource file, separated by semicolon (e.g., "offerId,slotId;offerId;./data-settings.json")
    default: ''
  previous_orders:
    description: Previous order numbers to cancel, separated by comma (eg. 100,102)
  min_rent_minutes:
    description: How long order should run
    default: '129600'
  rollingback_on_error:
    description: Enable rollback (cancel created orders) on workflow creation error
    required: false
    default: true
    type: boolean

outputs:
  order_ids:
    description: Created order ids
    value: ${{ steps.create-workflows.outputs.order_ids }}

runs:
  using: composite
  steps:
    - name: Check spctl
      uses: Super-Protocol/sp-build-tools/actions/check-spctl@v1
      with:
        spctl_config_file: ${{ inputs.spctl_config_file }}

    - name: Validate input
      shell: bash
      run: |
        if [ ! "$(echo ${{ inputs.solution }} | grep -E '[0-9]')" ]; then
          if [ ! -f "${{ inputs.solution }}" ]; then
            echo "Error: File specified by inputs.solution does not exist."
            exit 1
          fi
        fi

        # Split inputs.data into an array using a semicolon as a separator
        IFS=';' read -ra data_items <<< "${{ inputs.data }}"

        # Loop through each item in the array
        for item in "${data_items[@]}"; do
          # Check if the item contains only numbers and commas (comma-separated numbers)
          if ! [[ "$item" =~ ^[0-9,]+$ ]]; then
            # If it's not numbers with commas, check if it's a file path and if the file exists
            if [ ! -f "$item" ]; then
              echo "Error: File specified by inputs.data ($item) does not exist."
              exit 1
            fi
          fi
        done

        # Validate solution_configuration if provided
        if [ -n "${{ inputs.solution_configuration }}" ]; then
          # Split inputs.solution_configuration into an array using a comma as a separator
          IFS=',' read -ra config_items <<< "${{ inputs.solution_configuration }}"

          # Split inputs.tee_offers into an array using a comma as a separator
          IFS=',' read -ra tee_items <<< "${{ inputs.tee_offers }}"

          # If multiple configurations provided, count must match tee_offers count
          if [ ${#config_items[@]} -gt 1 ]; then
            if [ ${#config_items[@]} -ne ${#tee_items[@]} ]; then
              echo "Error: Number of solution_configuration items (${#config_items[@]}) must match number of tee_offers items (${#tee_items[@]})"
              exit 1
            fi
          fi

          # Check if each configuration file exists
          for config_item in "${config_items[@]}"; do
            if [ ! -f "$config_item" ]; then
              echo "Error: Configuration file specified by inputs.solution_configuration ($config_item) does not exist."
              exit 1
            fi
          done
        fi

    - name: Cancel previous orders
      shell: bash
      if: ${{ inputs.previous_orders != '' }}
      run: |
        # Split line like "order1,order2" by comma (,) and save orders to "orderIds" variable
        IFS=',' read -ra orderIds <<< "${{ inputs.previous_orders }}"

        for orderId in "${orderIds[@]}"; do
          echo "Cancelling order $orderId"
          ./spctl orders cancel $orderId --config ${{ inputs.spctl_config_file }} || true
        done

    - name: Create workflows
      id: create-workflows
      shell: bash
      run: |
        set -e
        orderIds=()

        IFS=',' read -ra teeOfferIds <<< "${{ inputs.tee_offers }}"

        # Prepare solution_configuration array
        if [ -n "${{ inputs.solution_configuration }}" ]; then
          IFS=',' read -ra configItems <<< "${{ inputs.solution_configuration }}"
        fi

        for i in "${!teeOfferIds[@]}"; do
          teeOffer="${teeOfferIds[$i]}"

          COMMAND="./spctl workflows create --tee $teeOffer"

          if [ -n "${{ inputs.base_image }}" ]; then
            COMMAND="$COMMAND --solution ${{ inputs.base_image }}"
          fi

          COMMAND="$COMMAND --solution ${{ inputs.solution }}"

          if [ -n "${{ inputs.solution_configuration }}" ]; then
            # If only one configuration, use it for all tee_offers
            if [ ${#configItems[@]} -eq 1 ]; then
              COMMAND="$COMMAND --solution-configuration ${configItems[0]}"
            else
              # Use configuration with corresponding index
              COMMAND="$COMMAND --solution-configuration ${configItems[$i]}"
            fi
          fi

          # Split inputs.data into an array using a semicolon as a separator
          IFS=';' read -ra data_items <<< "${{ inputs.data }}"

          # Loop through each item in the array
          for item in "${data_items[@]}"; do
            # Append "--data $item" to the COMMAND variable for each item
            COMMAND="$COMMAND --data $item"
          done

          COMMAND="$COMMAND --orders-limit 10000 --min-rent-minutes ${{ inputs.min_rent_minutes }} --config ${{ inputs.spctl_config_file }} > workflow_result.txt"

          echo "COMMAND=$COMMAND"
          set +e
          eval $COMMAND
          STATUS=$?
          set -e

          cat workflow_result.txt

          if [ $STATUS -ne 0 ] && [ "${{ inputs.rollingback_on_error }}" = "true" ]; then
            echo "Error: Workflow creation failed for tee $teeOffer. Rolling back..."
            # Cancel all previously created orders
            for cancel_id in "${orderIds[@]}"; do
              echo "Rolling back: cancelling order $cancel_id"
              ./spctl orders cancel $cancel_id --config ${{ inputs.spctl_config_file }} || true
            done
            echo "order_ids=" >> $GITHUB_OUTPUT
            echo "Workflow creation failed and rollback performed." >&2
            exit 1
          fi

          ORDER_ID=$(cat workflow_result.txt | grep -oP '\["\K\d+(?="\])')

          orderIds+=($ORDER_ID)
        done

        ORDER_IDS=$(IFS=','; echo "${orderIds[*]}")

        echo "order_ids=$ORDER_IDS" >> $GITHUB_OUTPUT
